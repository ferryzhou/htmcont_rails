<h1>Listing conts</h1>

<%= will_paginate %>

<table>
  <tr>
    <th>Title</th>
    <th>Error type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @conts.each do |cont| %>
  <tr>
    <td><%= link_to cont.title, cont.link %></td>
    <td><%= cont.error_type %></td>
    <td><%= link_to 'Show', cont %></td>
    <td><%= link_to 'Edit', edit_cont_path(cont) %></td>
    <td><%= link_to 'Destroy', cont, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<%= will_paginate %>

<br />

<%= link_to 'New Cont', new_cont_path %>
